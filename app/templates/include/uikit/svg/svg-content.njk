<p class="description-type">
    To display an example of inline SVG icons, update the array in the <span class="file-code">global.json</span> file to the <span class="function-code">`page.uikit.svgicons.icons`</span> object. <br /><br />
    &bull; Each element in the array corresponds to the name of the svg file. Also check the path of the correct spritemap in the distribution folder in the object: <span class="function-code">`page.uikit.svgicons.spritepath`</span>. <br /><br />
    &bull; The colors of the icons are just an example. It is necessary to create additional classes to draw up the desired effect. Make sure you have correctly enabled the Gulp task for svg. <br />
    {% if page.uikit.svgicons.showcollection %}
        <br />
        {% set examplefile = page.uikit.svgicons.examplefile %}
        &bull; To view the entire list of svg icons installed consult <a href="{{examplefile}}">this</a> page. Be sure your configuration gulp task <code class="function-code">svgOptions.exampleFile</code> is set to <code class="function-code">true</code>.
    {% endif %}
</p>

{% if page.uikit.svgicons.switcher.show %}
    <div id="uikit-switch-render">
        <span class="info">Toggle for show true tone on icons. <br /> (Only if the color is available)</span>
        <div id="uikit-switch-item">
            <input type="checkbox" id="uikit-toggle-truetone" name="uikit-toggle">
            <div class="toggle">
                <label for="uikit-toggle-truetone">
                    <i></i>
                </label>
            </div>
        </div>
    </div>
{% endif %}

<ul id="uikit-svg-list" class="svg-list">
    {% set svgpath = page.uikit.svgicons.spritepath %}
    {% set icons = page.uikit.svgicons.icons %}
    {% set size = page.uikit.svgicons.size %}

    {% for icon in icons %}
        <li>
            {% if page.uikit.svgicons.switcher.show and icon | lower == 'instagram' %}
                <svg width="0" height="0" class="visuallyhidden">
                    <radialGradient id="instagram-gradient" r="150%" cx="30%" cy="107%">
                        <stop stop-color="#fdf497" offset="0" />
                        <stop stop-color="#fdf497" offset="0.05" />
                        <stop stop-color="#fd5949" offset="0.45" />
                        <stop stop-color="#d6249f" offset="0.6" />
                        <stop stop-color="#285AEB" offset="0.9" />
                    </radialGradient>
                </svg>
            {% endif %}
            <svg role="img" title="{{icon | lower}}" class="{{icon | lower}} {{size}}">
                <use xlink:href="{{svgpath}}#{{icon | lower}}"/>
            </svg>
        </li>
    {% endfor %}
</ul>
<script type="text/javascript">
    (function() {
       svg4everybody();
    })();
</script>
