<link href="https://fonts.googleapis.com/css?family=Abril+Fatface" rel="stylesheet">
<style id="uikit-render">
body {
    background-image: linear-gradient(120deg, #fdfbfb 0%, #ebedee 100%);
}

main {
    overflow: hidden;
}

.section-type {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
}

.section-title {
    font-family: 'Abril Fatface', cursive;
    font-size: 280px;
    font-size: calc((112.5% + 15vw) * 2);
    position: absolute;
    top: 0;
    left: -15px;
    text-transform: none;
    letter-spacing: 12px;
    margin: 0;
    opacity: 0.03;
    pointer-events: none;
    z-index: -1;
}
</style>
